server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: ecommerce-semantic-search

  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:

      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
        physical_strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    clean-on-validation-error: true
    clean-disabled: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: levuhung678@gmail.com
    password: xcmb ajlr wcaz zgdk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  jwt:
    secret: "491aee2a-0b08-444e-af9a-cbc250be22aa"
    expiration: 86400000

logging:
  level:
    root: INFO
    com.ecommerce: DEBUG
    org.springframework.security: INFO